---
import Page from '@/layouts/Page.astro';
import PlaceGrid from '@/components/PlaceGrid.astro';
import ArticleCard from '@/components/ArticleCard.astro';
import { getTown } from '@/config';
import { getTownEntries } from '@/lib/content';
import { THINGS_TO_DO_TYPES } from '@/content/schemas';

const town = getTown();
const places = (await getTownEntries('places')).filter((p) => THINGS_TO_DO_TYPES.includes(p.data.type));
places.sort((a, b) => Number(b.data.featured) - Number(a.data.featured) || a.data.title.localeCompare(b.data.title));
const outdoors = (await getTownEntries('articles')).filter(
  (a) => a.data.tags.includes('outdoors') || a.data.category.toLowerCase() === 'outdoors',
);
---
<Page title="Things to Do" eyebrow={town.name} description={`Trails, parks and venues in and around ${town.name}, ${town.state}.`} intro={`Trails, parks and places worth an afternoon in and around ${town.name}.`}>
  <PlaceGrid places={places} filterable emptyMessage="No trails, parks or venues listed yet." />
  {outdoors.length > 0 && (
    <section class="mt-20">
      <p class="eyebrow mb-8 border-b border-line-soft pb-4">Outdoors, in depth</p>
      <div class="grid gap-x-8 gap-y-10 md:grid-cols-2">
        {outdoors.map((article) => <ArticleCard article={article} />)}
      </div>
    </section>
  )}
</Page>
