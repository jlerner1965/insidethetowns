{
  "name": "inside-the-towns",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "One codebase, many Front Range town guides. Select the site with the TOWN env var.",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "dev": "node scripts/run.ts dev",
    "build": "node scripts/run.ts build",
    "preview": "node scripts/run.ts preview",
    "check": "astro check",
    "validate": "node scripts/validate-content.ts",
    "new-town": "node scripts/new-town.ts",
    "weekly": "node scripts/weekly.ts",
    "import-events": "node scripts/import-events.ts",
    "dev:niwot": "TOWN=niwot astro dev",
    "dev:hub": "TOWN=hub astro dev",
    "build:niwot": "TOWN=niwot node scripts/run.ts build",
    "build:hub": "TOWN=hub node scripts/run.ts build",
    "astro": "astro",
    "check-colors": "node scripts/check-colors.ts"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "@fontsource-variable/fraunces": "^5.3.0",
    "@fontsource-variable/instrument-sans": "^5.3.0",
    "@tailwindcss/vite": "^4.3.3",
    "@vercel/analytics": "^2.0.1",
    "astro": "^7.3.3",
    "tailwindcss": "^4.3.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^6.0.3"
  }
}
